# Python Setup

The devcontainer includes Python and Jupyter. If running locally:

```bash
python -m venv .venv
source .venv/bin/activate  # macOS/Linux
# .venv\Scriptsctivate   # Windows
python -m pip install --upgrade pip
```
